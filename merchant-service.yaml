server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pg-merchant-db?currentSchema=merchant_service
    username: ${PSQL_USER}
    password: ${PSQL_PASSWORD}

jwt:
  secret-key: ${JWT_SECRET}

webhook:
  secret-encryption-key: ${WEBHOOK_SECRET}